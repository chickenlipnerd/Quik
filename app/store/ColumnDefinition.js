Ext.define('QS.store.ColumnDefinition', {
    extend: 'Ext.data.Store',
    model: 'QS.model.ColumnDefinition',
    autoLoad: false,
    stateful: false,
    
    //Could override the models default proxi here
});
